/**
 * Change features property names.
 * @export
 * @interface ApChangeDetectionOptions
 */
export interface ApChangeDetectionOptions {
  detach?: string;
  detect?: string;
  properties?: string;
  reattach?: string;
}
