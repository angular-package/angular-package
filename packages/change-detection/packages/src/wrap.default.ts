import { ApChangeDetectionOptions } from '../interface';

export const WRAP_DEFAULT: ApChangeDetectionOptions = {
  detach: '_detach',
  detect: '_detect',
  properties: '_properties',
  reattach: '_reattach'
};
